# ~/.config/hypr/hyprpaper.conf
#
# ╔════════════════════════════════════════════════════════════════════════╗
# ║  THIS FILE IS YOURS.                                                   ║
# ║  LYNX created it once and will never touch it again.                   ║
# ╚════════════════════════════════════════════════════════════════════════╝
#
# Pick wallpapers with `lx wallpaper` (SUPER + W), not by editing this file:
# the current choice is runtime state that LYNX keeps in ~/.cache/lynx.

source = ~/dotfiles/lynx/config/hypr/hyprpaper.conf

# ─── Your overrides ─────────────────────────────────────────────────────────
# hyprpaper reads top to bottom, so settings here replace LYNX's.
#
# splash = true
